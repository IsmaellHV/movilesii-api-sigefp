{
  "name": "api-proyecto2025",
  "version": "1.0.0",
  "description": "API REST para sistema de gestión financiera personal",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "docker": "rm -rf build && tsc --project ./ && cp -a ./src/assets ./build/src && docker image rm -f api-sigecomer-backend-all && docker build -t api-sigecomer-backend-all .",
    "publish": "rm -f api-sigecomer-backend-all.tgz && docker save api-sigecomer-backend-all | gzip > api-sigecomer-backend-all.tgz",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api",
    "rest",
    "express",
    "mysql",
    "finanzas",
    "nodejs"
  ],
  "author": "CIBERTEC - Desarrollo de Aplicaciones Móviles II",
  "license": "ISC",
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "axios": "^1.11.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.1",
    "mysql2": "^3.6.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
